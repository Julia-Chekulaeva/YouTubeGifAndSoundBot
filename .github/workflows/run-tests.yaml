name: run-tests-task
on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
  workflow_dispatch:


jobs:
  Run-Tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]

    steps:
      - uses: actions/checkout@HEAD
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: FedericoCarboni/setup-ffmpeg@v2
        id: setup-ffmpeg
      - run: mkdir ./src/test/resources
      - run: pwd
      - run: ls -R
      - run: mkdir /home/runner/work/YouTubeGifAndSoundBot/YouTubeGifAndSoundBot/src/test/resources
      - run: ls -l /home/runner/work/YouTubeGifAndSoundBot/YouTubeGifAndSoundBot/src/test/resources
      - name: Build with Gradle
        env:
          ffmpeg_path: ${{ steps.setup-ffmpeg.outputs.ffmpeg-path }}
          ffprobe_path: ${{ steps.setup-ffmpeg.outputs.ffprobe-path }}
        uses: gradle/gradle-build-action@v2
        with:
          arguments: test -i
          gradle-version: 7.4.2